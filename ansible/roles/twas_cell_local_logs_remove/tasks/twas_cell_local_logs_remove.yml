---

- name: Clear local log directory
  delegate_to: localhost
  ansible.builtin.file:
    path: "{{ logsDir }}/{{ env_name }}"
    state: absent

- name: Create local log directory
  delegate_to: localhost
  ansible.builtin.file:
    path: "{{ logsDir }}/{{ env_name }}"
    state: directory
