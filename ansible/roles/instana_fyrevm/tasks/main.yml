---

- name: set jmeter host specs
  no_log: "{{ noLog }}"
  vars:
  ## crc host specs
  - arch: x
  - numcpu: 2
  - memory: 8 # GB memory Fyre hosting VM
  - disksizegb: 1024 # extra DB
  - fos: 'Ubuntu 20.04'
  - comment: 'Instana Host'
  - localclusterName: "{{ clusterName }}"
  include_tasks:
    file: instana_fyrevm.yml
