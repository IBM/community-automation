---
- name: set wlp_usr_dir
  set_fact:
   wlp_usr_dir: "{{ install_base }}/{{ liberty_dir }}/usr"
  when: ansible_system != 'OS400'
- name: set OS400 wlp_usr_dir
  set_fact: 
   wlp_usr_dir: "/QIBM/UserData/WebSphere/Liberty/V85/ND/wlp/usr"
  when: ansible_system == 'OS400'

