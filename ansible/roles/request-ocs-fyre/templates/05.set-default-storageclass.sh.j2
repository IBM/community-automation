#!/bin/bash

my_dir=$(dirname $(readlink -f $0))
default_sc="{{ setdefault }}"
if [ "$default_sc" == "True" ]; then
   oc patch storageclass ocs-storagecluster-cephfs -p '{"metadata": {"annotations":{"storageclass.kubernetes.io/is-default-class":"true"}}}'
   oc get sc
fi
